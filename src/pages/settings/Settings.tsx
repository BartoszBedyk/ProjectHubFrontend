import React from 'react';
import CustomLayout from "../../components/Layout/Layout";

const Settings = () => {
    return (
        <CustomLayout>
            <div>
                HALO HALO TEST SETTINGS
            </div>
        </CustomLayout>
    );
};

export default Settings;